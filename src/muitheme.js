import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    props: {
    },
    palette: {
        primary: {
            main: '#ffffff',
        },
        secondary: {
            main: '#3a3569',
        },
    },
    overrides: {
    },
});

export default theme;
